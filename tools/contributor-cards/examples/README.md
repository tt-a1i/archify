# Real PR example

This is a snapshot of merged PR [#394](https://github.com/tt-a1i/archify/pull/394), generated on 2026-09-14 from GitHub API metadata using the contributor-card CLI. The image was visually checked at 1000 × 1500. It is an example output, not an instruction to send a reply or reissue a card. The adjacent JSON preserves source metadata and PNG SHA-256.

Regenerate with:

```sh
node tools/contributor-cards/cli.mjs --pr 394 --out /tmp/archify-card
```
