# Main integration verification — 2026-09-11

Integrated upstream `8c3af8a11318d3e0f46bb123149c4863d358f830` into PR #362,
retaining the original `43abaa4` commits. Upstream extracts Viewer maintenance
sources into `viewer/` and refreshes sponsor content. The PR's atlas runtime,
CLI, sequence metrics and browser readability implementation remain unchanged.

The five conflicts were generated artifacts: `archify.zip`, README showcase
GIF/receipt, Gallery HTML and Gallery manifest. They were rebuilt from the
combined source rather than selecting one side's generated bytes. Both packaged
and repository examples were regenerated; the assembled Viewer freshness check
passed. Gallery validation reports 11 artifacts and 99 checks. The ZIP uses the
canonical Node 22 toolchain.

Fresh focused CLI/sequence tests: 12 passed. Fresh real Chrome tests: 4 passed
across `atlas-browser.test.mjs` and `desktop-reader-browser.test.mjs`. These
exercise offline detail entry/choice, return camera/focus, theme synchronization,
icons, canonical SVG export, intentionally unreadable messages, four desktop
sizes, all packaged examples and production architecture readability.

Fresh perceptual review: [light](merge-layered-light.png) and
[dark](merge-layered-dark.png) captures were inspected after the integration at
2048×1320. The graph remains contained, labels and navigation controls are
visible, and the source's authored topology is retained. These captures are
bounded fixture evidence, independent of the earlier before/after comparison.

The full-suite result and final merge identity are recorded in the PR's Tests
run section. Remote CI is independent of these local checks.
