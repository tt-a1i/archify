# Queue Gateway fixture

This bounded fixture is a small repository-backed job gateway. The source is
split between the request entry point, deploy-time configuration, and runtime
queue/worker modules. It has no install or start step; the source files are
the reproducibility snapshot used by the benchmark.
