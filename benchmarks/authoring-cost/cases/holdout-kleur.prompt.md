# kleur module architecture

Read this repository and create an architecture diagram of the terminal color
formatting library. Show how process environment detection controls whether
formatting is enabled, how the default fluent API builds a chain of styles,
and how chained calls produce formatted text. Also show the separate named
color export surface and its formatting behavior.
