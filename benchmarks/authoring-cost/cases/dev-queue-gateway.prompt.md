# Queue gateway architecture

Create an architecture diagram from this repository's source. Show how a
tenant request enters the gateway, waits in a queue, and is
processed by workers. Make the configured authentication, billing, and result
boundaries visible. Keep the concurrency limit and the dead-letter failure
path legible, including the retry relationship.
