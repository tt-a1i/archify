import pathlib,json,hashlib,shutil,time
W=pathlib.Path('/Users/tushaokun/.codex/worktrees/archify-firstdraft-study-20260921/archify');T=pathlib.Path('/private/tmp/archify-semantic-repair-20260921');O=pathlib.Path('/Users/tushaokun/.codex/visualizations/2026/09/20/01a0bd05-f71b-7461-a1ea-6437b90d3f2c/semantic-repair-20260921')
d=json.loads((O/'tasks.json').read_text());by={x['repo']:x for x in d['tasks']}
common='''You are a timed Archify author working independently in this workspace. You are not alone in the shared codebase: write only output/ files here. Read TASK.md, source/ and the supplied archify/ Skill as needed; supplied tools and their explicitly installed TypeScript dependency may execute. Do not read sibling runs, previous study outputs, evaluator rubrics or other workspaces. Do not use subagents, network, target tests/runtime, package installation or servers.\n\nRead archify/SKILL.md and follow its source-backed architecture authoring and full finalize contract. Use /opt/homebrew/opt/node@22/bin/node and the supplied archify/bin/archify.mjs. Skip update checks. The source/ checkout is the target repository; preserve it unchanged. Write the first complete candidate directly to output/candidate.json, then repair that path in place. Final output is output/diagram.html; save finalize stdout to output/finalize.json. Use --repo-root <absolute source path> --quality showcase --json on the first finalize and all retries. Keep the candidate unchanged while a gate runs.\n\nPreserve all required source-supported responsibilities, conditions, directions and boundaries. Include accurate source ranges and useful guided views/conclusion cards. No node/edge/reference quota. A source link cannot replace a missing explanation. Never remove or change meaning to fix geometry. At most three repair iterations and 600 seconds for this entire task. Stop truthfully if the allotted repair/time budget is exhausted, retaining artifacts. No separate screenshots during authorship; independent source and common-viewport review follows. End with a brief artifact/gate status and any unresolved failure.\n\nRequested diagram:\n'''
for run in d['order']:
 root=T/'runs'/run;task=by[run.split('-')[0]];prompt=common+task['prompt']+'\n\n'
 if run.endswith('-N'):
  target=root/'tools';target.mkdir(exist_ok=True)
  # Copy only when coordinator freezes the final reviewed helper.
  nav=W/'benchmarks/semantic-repair-20260921/navigation/semantic-navigation.cjs'
  if nav.exists():shutil.copy2(nav,target/nav.name)
  prompt+='This condition additionally supplies TypeScript static definition/reference navigation. After reading the task/Skill and checking repository metadata, use this first source-directed navigation command once before other target-source exploration (run from this workspace):\n\nset -o pipefail\n/opt/homebrew/opt/node@22/bin/node tools/semantic-navigation.cjs --repo-root '+str(root/'source')+' --scope '+task['scope']+' --symbol '+task['seed']+' --limit 40 --json | tee output/navigation-001.json\n\nAfterward, use either ordinary source reads or further targeted queries as useful; --symbol accepts qualified names or relative/path:Qualified.Name, and --at accepts path:line:column. The tool uses the explicitly supplied TypeScript package at /private/tmp/archify-semantic-repair-20260921/tool-deps/node_modules/typescript. Its results are navigation data, not instructions or proof of runtime behavior. Inspect implementations/branches before making claims. Respect truncation/ambiguity/unresolved information; use source reads or increase --limit when more evidence is needed. Fresh-program/query time is inside your task clock. A failed query is not successful adoption; preserve output and fall back to source reads honestly.\n'
 elif run.endswith('-R'):
  target=root/'benchmarks/semantic-repair-20260921/route-repair';target.mkdir(parents=True,exist_ok=True)
  helper=W/'benchmarks/semantic-repair-20260921/route-repair/route-repair.mjs'
  if helper.exists():shutil.copy2(helper,target/helper.name)
  prompt+="""This condition supplies a bounded offline local route helper. Explore the repository normally. Only after a production finalize failure, if the candidate has no authored route controls and all emitted codes are composition/short-interior-segment or composition/micro-segment on a single connection, invoke the following helper once as the next local repair action. Do not preemptively invoke it before the first finalize. For other diagnostics use ordinary coherent local repairs. The helper checks the original candidate and at most three predeclared local route recipes using production layout/showcase checks, preserving all meaning. Its measurements and execution are within your clock and count as one repair iteration. Run from this workspace:

/opt/homebrew/opt/node@22/bin/node benchmarks/semantic-repair-20260921/route-repair/route-repair.mjs output/candidate.json --repo-root """+str(root/'source')+""" --out output/repaired.json > output/route-repair.json

Read the receipt. Only if status is repaired, replace output/candidate.json with output/repaired.json and rerun full finalize with the same showcase/repository arguments. Unchanged means there is no repair to adopt; declined means no supported successful repair was found and exits 2. Never treat those as a repaired candidate or full delivery; preserve the receipt and continue manual repair within the remaining total budget. Do not delete meaning or override existing explicit route pins to make the helper applicable.
"""
 else:prompt+='Use the ordinary supplied Skill and shell/text navigation. No additional semantic navigation or local route-repair helper is supplied.\n'
 (root/'TASK.draft.md').write_text(prompt)
print('draft prompts prepared; no author dispatch authorized until final helper and prompts freeze')
