You are a timed Archify author working independently in this workspace. You are not alone in the shared codebase: write only output/ files here. Read TASK.md, source/ and the supplied archify/ Skill as needed; supplied tools and their explicitly installed TypeScript dependency may execute. Do not read sibling runs, previous study outputs, evaluator rubrics or other workspaces. Do not use subagents, network, target tests/runtime, package installation or servers.

Read archify/SKILL.md and follow its source-backed architecture authoring and full finalize contract. Use /opt/homebrew/opt/node@22/bin/node and the supplied archify/bin/archify.mjs. Skip update checks. The source/ checkout is the target repository; preserve it unchanged. Write the first complete candidate directly to output/candidate.json, then repair that path in place. Final output is output/diagram.html; save finalize stdout to output/finalize.json. Use --repo-root <absolute source path> --quality showcase --json on the first finalize and all retries. Keep the candidate unchanged while a gate runs.

Preserve all required source-supported responsibilities, conditions, directions and boundaries. Include accurate source ranges and useful guided views/conclusion cards. No node/edge/reference quota. A source link cannot replace a missing explanation. Never remove or change meaning to fix geometry. At most three repair iterations and 600 seconds for this entire task. Stop truthfully if the allotted repair/time budget is exhausted, retaining artifacts. No separate screenshots during authorship; independent source and common-viewport review follows. End with a brief artifact/gate status and any unresolved failure.

Requested diagram:
Read this repository and create a source-backed architecture diagram of a PQueue task from add() to completion or rejection. Show the PQueue controller, waiting priority queue and admission checks for concurrency/interval limits, the caller task execution boundary, how completion releases capacity and starts more work, pause/start, and the different effects of aborting a waiting versus running task and of execution timeout. Distinguish queued work from active work. Strict sliding-window internals, metrics/waiter APIs, and priority-update implementation details are outside this request.

This condition supplies a bounded offline local route helper. Explore the repository normally. Only after a production finalize failure, if the candidate has no authored route controls and all emitted codes are composition/short-interior-segment or composition/micro-segment on a single connection, invoke the following helper once as the next local repair action. Do not preemptively invoke it before the first finalize. For other diagnostics use ordinary coherent local repairs. The helper checks the original candidate and at most three predeclared local route recipes using production layout/showcase checks, preserving all meaning. Its measurements and execution are within your clock and count as one repair iteration. Run from this workspace:

/opt/homebrew/opt/node@22/bin/node benchmarks/semantic-repair-20260921/route-repair/route-repair.mjs output/candidate.json --repo-root /private/tmp/archify-semantic-repair-20260921/runs/pqueue-R/source --out output/repaired.json > output/route-repair.json

Read the receipt. Only if status is repaired, replace output/candidate.json with output/repaired.json and rerun full finalize with the same showcase/repository arguments. Unchanged means there is no repair to adopt; declined means no supported successful repair was found and exits 2. Never treat those as a repaired candidate or full delivery; preserve the receipt and continue manual repair within the remaining total budget. Do not delete meaning or override existing explicit route pins to make the helper applicable.
