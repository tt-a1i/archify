# Runtime study evidence

Frozen source revisions, inputs, metrics and independent reviews are kept here. Full per-run first/intermediate/final JSON bytes, machine receipts, TASK/build inputs and public native timing metadata are preserved in `../runtime-records.tar.gz`. The sibling manifest records every member SHA-256 and the archive SHA-256; all were read back and checked after writing. Selected compact JSON files remain unpacked for the portable regression tests.

Extract from the parent `evidence` directory with `tar -xzf runtime-records.tar.gz`. Files use the `runtime/` prefix. No raw session, reasoning text or tool payload is archived. A failed author has no accepted delivery latency. Post-run captures and exact-byte replay are not timed author successes.

Full HTML and PNG captures remain at:

`/Users/tushaokun/.codex/visualizations/2026/09/20/01a0bd05-f71b-7461-a1ea-6437b90d3f2c/semantic-firstdraft-20260921/runtime-trials`

Two blind source/image reviews are complete. All twelve original runs passed the later fail-closed integrity audit, so that harness correction changed no observation. Both experiment screens failed and neither triggered confirmation.
